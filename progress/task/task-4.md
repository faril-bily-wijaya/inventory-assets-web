# Task 4: Update DeviceModal

**Files:**
- Modify: `frontend/src/components/modals/DeviceModal.tsx`

**Action:** Add form fields for the 4 new organization fields.

**Steps:**
1. Add new fields to `deviceSchema`
2. Add `defaultValues` in `useForm`
3. Add form fields to JSX
