# Task 2: Endpoint Template Khusus Genset

**Tujuan:**
Mengubah `backend/src/routes/devices.routes.ts` pada endpoint `GET /api/devices/import/template` agar merespons parameter `?type=genset`. Jika `type=genset`, kembalikan template Excel khusus yang memuat kolom-kolom persis seperti `Book1.xlsx` (ditambah Latitude & Longitude).

**Status:** DONE
