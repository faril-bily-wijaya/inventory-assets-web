# Task 1: Membuat Logika Fuzzy Match Menjadi Case-Insensitive

**Tujuan:**
Mengubah `backend/src/services/importService.ts` agar pemetaan `distByName`, `clusterByName`, dll. menggunakan huruf kecil (`toLowerCase()`) sehingga tahan terhadap perbedaan format penulisan antara file utama dan file Genset.

**Status:** [x] DONE
