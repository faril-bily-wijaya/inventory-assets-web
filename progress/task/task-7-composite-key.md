# Task 1: Update Logika Pemetaan di `importService.ts`

**Tujuan:**
Menggunakan kunci kombinasi (Composite Key) untuk `regByName`, `distByName`, dan `clusterByName` agar terhindar dari bentrokan hierarki ketika nama hierarki sama (contohnya 'CLUSTER DEFAULT').

**Status:** [x] DONE
