# Task 4 Report: Update DeviceModal

## What was done:
- Updated `frontend/src/components/modals/DeviceModal.tsx`.
- Added `uuid`, `organizationName`, `organizationUuid`, and `organizationSname` fields to `deviceSchema`.
- Updated `defaultValues` in the `useForm` hook to map the new fields.
- Added a new UI section "Organization Info" to the modal containing `Input` fields for these 4 organization fields.

Task 4 is complete.
