# Task 5 Report: Update DevicesPage Table

## What was done:
- Updated `frontend/src/pages/DevicesPage.tsx`.
- Added an `Organization` column to the `devices` table header.
- Rendered `device.organizationSname` and `device.organizationName` in the new column for each row.
- Updated the `colSpan` for the loading and empty states from 7 to 15 to account for the new and existing columns.

Task 5 is complete.
